#include "factorial.h"
#include<stdio.h>
int factorial(int operand)
{
    if(operand < 0)
    return 1;
    else{
        for(i=1;i<=operand1;++i){
            fact*=i;
        }
        return fact;
    }
    return 0;
}